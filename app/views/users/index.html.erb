<% if logged_in? %>
    <ul>LIST OF USERS:
        <% @users.each do |user| %>
            <li><%= link_to "#{user.username}", user_path(user) %></li>
        <% end %>
    </ul>
<% else %>
    <p>Please log in to view this list.</p>
<% end %>